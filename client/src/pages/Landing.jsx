import { Link } from 'react-router-dom';
import { motion } from 'framer-motion';
import { Fingerprint, KeyRound, LockKeyhole, ShieldCheck } from 'lucide-react';
export function Landing() { return <main className="landing"><section className="hero"><div className="hero-copy"><div className="brand hero-brand"><span className="brand-mark"><LockKeyhole size={20} /></span><span>VaultShare</span></div><motion.h1 initial={{ opacity: 0, y: 16 }} animate={{ opacity: 1, y: 0 }}>End-to-end secure file sharing for teams that cannot afford leaks.</motion.h1><p>Encrypt uploads, issue expiring links, track access, manage workspaces, and investigate every file event from one premium security cockpit.</p><div className="hero-actions"><Link className="button primary" to="/register">Create secure vault</Link><Link className="button ghost" to="/login">Sign in</Link></div></div><div className="security-visual glass-panel"><div className="vault-door"><ShieldCheck size={72} /><span>AES-256-GCM</span></div><div className="visual-grid"><p><Fingerprint /> Device verified</p><p><KeyRound /> Per-file keys</p><p><ShieldCheck /> Signed links</p></div></div></section></main>; }
